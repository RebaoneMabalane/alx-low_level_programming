#include "main.h"

/**
 * main - check the code
 * Return: Always 0.
 */
char *string_tropper(char *n)
{
	int i;

	i = 0;
	while (n[i] != '\0')
	{
		if (n[i] >= 'a' && n[i] <= 'z')
			n[i] = n[i] - 32;
		i++;
	}
	return (n);
}
